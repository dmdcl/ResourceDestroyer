policies:
  # --------------------------------------------------------
  # STEP 1: The Mark (30-Day Warning)
  # --------------------------------------------------------
  - name: client-elbv2-targetgroup-inventory-mark
    resource: app-elb-target-group
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${MARK_ROLE_ARN}"
      packages: [jwt]
    filters:
      - "tag:keep": absent
      - "tag:maid_status": absent
    actions:
      - type: mark-for-op
        op: delete
        days: 30
      - type: notify
        template: default.html
        priority_header: '2'
        subject: "Action Required: ELB Target Group Scheduled for Deletion in 30 Days"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"

  # --------------------------------------------------------
  # STEP 2: The Skew (1-Day Final Warning)
  # --------------------------------------------------------
  - name: client-elbv2-targetgroup-inventory-notify-warning
    resource: app-elb-target-group
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${MARK_ROLE_ARN}"
    filters:
      - "tag:keep": absent
      - type: marked-for-op
        op: delete
        skew: 1
      - type: value
        key: LoadBalancerArns
        value: []
        op: eq
    actions:
      - type: notify
        template: default.html
        priority_header: '1'
        subject: "FINAL WARNING: ELB Target Group Will Be Deleted TOMORROW"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"

  # --------------------------------------------------------
  # STEP 3: The Cleanup (Execution & Final Notification)
  # --------------------------------------------------------
  - name: client-elbv2-targetgroup-inventory-cleanup
    resource: app-elb-target-group
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${CLEANUP_ROLE_ARN}"
    filters:
      - type: marked-for-op
        op: delete
      - "tag:keep": absent
      - type: value
        key: LoadBalancerArns
        value: []
        op: eq
    actions:
      - type: delete
      - type: notify
        template: default.html
        priority_header: '1'
        subject: "Alert: ELB Target Group Has Been Deleted"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"
