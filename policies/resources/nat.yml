policies:
  # --------------------------------------------------------
  # STEP 1: The Mark (30-Day Warning)
  # --------------------------------------------------------
  - name: client-nat-inventory-mark
    resource: nat-gateway
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${MARK_ROLE_ARN}"
      packages: [jwt]
    filters:
      - "tag:keep": absent
      - "tag:maid_status": absent
      - type: value
        key: State
        op: in
        value:
          - available
          - failed
    actions:
      - type: mark-for-op
        op: delete
        days: 30
      - type: notify
        template: default.html
        priority_header: '2'
        subject: "Action Required: NAT Gateway Scheduled for Deletion in 30 Days"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"

  # --------------------------------------------------------
  # STEP 2: The Skew (1-Day Final Warning)
  # --------------------------------------------------------
  - name: client-nat-inventory-notify-warning
    resource: nat-gateway
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${MARK_ROLE_ARN}"
    filters:
      - "tag:keep": absent
      - type: value
        key: State
        op: in
        value:
          - available
          - failed
      - type: marked-for-op
        op: delete
        skew: 1
    actions:
      - type: notify
        template: default.html
        priority_header: '1'
        subject: "FINAL WARNING: NAT Gateway Will Be Deleted TOMORROW"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"

  # --------------------------------------------------------
  # STEP 3: The Cleanup (Execution & Final Notification)
  # --------------------------------------------------------
  - name: client-nat-inventory-cleanup
    resource: nat-gateway
    mode:
      type: periodic
      schedule: "rate(1 day)"
      role: "${CLEANUP_ROLE_ARN}"
    filters:
      - type: marked-for-op
        op: delete
      - "tag:keep": absent
    actions:
      - type: delete
      - type: notify
        template: default.html
        priority_header: '1'
        subject: "Alert: NAT Gateway Has Been Deleted"
        to:
          - "${ALERT_EMAIL}"
        transport:
          type: sqs
          queue: "${SQS_QUEUE_URL}"
